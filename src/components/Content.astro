---
import ParallaxLayer from './ParallaxLayer.astro'

interface Props {
  speed: number
  offset: number
  factor?: number
  class?: string
}

const { speed, offset, factor = 1, class: className = '' } = Astro.props
---

<ParallaxLayer
  class={`z-50 flex flex-col items-center justify-center p-4 sm:p-8 md:p-16${className ? ` ${className}` : ''}`}
  speed={speed}
  offset={offset}
  factor={factor}
>
  <slot />
</ParallaxLayer>
