---
import { Image } from 'astro:assets'
import type { ImageMetadata } from 'astro'

interface Props {
  link: string
  title: string
  children?: unknown
  image: ImageMetadata
  bg: string
}

const { link, title, image, bg } = Astro.props
---

<a
  href={link}
  target="_blank"
  rel="noreferrer noopener"
  class="relative block w-full rounded-lg px-4 py-2 text-white no-underline shadow-lg [transition:all_0.4s_cubic-bezier(0.175,0.885,0.32,1.275)] hover:-translate-y-1 hover:text-white hover:shadow-xl xs:px-8 xs:py-4"
  style={`background: ${bg || 'none'};`}
>
  <Image
    src={image}
    alt={title}
    width="800"
    height="533"
    loading="eager"
    class="h-auto w-full"
  />
  <div class="pt-2 text-2xl leading-none font-medium tracking-wide xs:text-3xl">
    {title}
  </div>
  <div class="text-sm opacity-80 [text-shadow:0_2px_10px_rgba(0,0,0,0.3)] xs:text-base">
    <slot />
  </div>
</a>
