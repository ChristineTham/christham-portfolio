---
interface Props {
  offset: number
  speed: number
  factor?: number
  class?: string
  style?: string
}

const { offset, speed, factor = 1, class: className = '', style = '' } = Astro.props
const initialTranslate = offset * speed * 100
---

<div
  data-parallax-layer
  data-offset={offset}
  data-speed={speed}
  style={`position: absolute; top: ${offset * 100}vh; height: ${factor * 100}vh; width: 100%; transform: translate3d(0, ${initialTranslate}vh, 0); will-change: transform; ${style}`}
  class={className}
>
  <slot />
</div>
